'use client';

import React from 'react';

export const AsideHome: React.FC = ({ }) => {

    return (
        <div defaultValue="account" className="h-max w-[20%] grow bg-white">
            asdasd
        </div>
    );
};
